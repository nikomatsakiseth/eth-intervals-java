package inter.compiler

import java.io.File

/** Support code for Symbol.ClassFromClassFile: 
  * Creates symbols from class files. */
object LoadClassFile {
    def apply(state: CompilationState, sym: Symbol.ClassFromClassFile) {
        throw new RuntimeException("TODO-- Implement .class file loading!")
    }
}