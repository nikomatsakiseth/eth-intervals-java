package inter.compiler

import Hl.{RN => in}
import Hl.{TI => out}
import Util._

object InferTypesPass {
    
    class LookupTable {
        
    }
    
    def apply(state: CompilationState, compUnit: in.CompUnit) {
        
    }
    
}