class Basic(Interval par) {
    [var]
    int aRes;
    
    interval a(par) {
        aRes = 22;
        this aRes = 22;
    }
    
    interval b(par) {
        use(aRes);
        use(this aRes);
    }
    
    a -> b;
}

class BigMethods(Interval par) {
    
    void use(int res) with(String str) {
        x = (1, 2, 3);
        (Integer x, String y) = msg();
        cond if {x} else {z};
    }
    
    interval b(par) {
        use(aRes) with("a string");
    }
    
    a -> b;
    
}
