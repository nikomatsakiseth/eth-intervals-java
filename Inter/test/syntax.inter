class Basic(Interval par) {
    [var]
    int aRes;
    
    interval a(par) {
        aRes = 22;
    }
    
    interval b(par) {
        use(aRes);
    }
    
    a -> b;
}

class BigMethods(Interval par) {
    
    void use(int res) with(String str) {
        x = (1, 2, 3);
        return if(cond) x else z;
    }
    
    interval b(par) {
        use(aRes) with("a string");
    }
    
    a -> b;
    
}
