package lower;

class LowerOrderedMethods {
    
    quadruple(Integer n) {
        double(n) + double(n)
    }
    
    double(Integer n) {
        n + n
    }
    
    Void errorGenerator(Integer m) {
        (quadruple(m)) badMethod() // ERROR no.such.method((java.lang.Integer), badMethod())
    }
    
}
// COMPILE