package resolve;

class Resolve2A() {
    Resolve2A refA;
    Resolve2B refB;
    Resolve2C refC; // ERROR cannot.resolve(Resolve2C)
}

class Resolve2B() {
    Resolve2A refA;
    Resolve2B refB;
    Resolve2C refC; // ERROR cannot.resolve(Resolve2C)
}

// COMPILE