package resolve;

class Resolve2A() {
    refA: Resolve2A;
    refB: Resolve2B;
    refC: Resolve2C; // ERROR cannot.resolve(Resolve2C)
}

class Resolve2B() {
    refA: Resolve2A;
    refB: Resolve2B;
    refC: Resolve2C; // ERROR cannot.resolve(Resolve2C)
}

// COMPILE