package resolve;

import some.cls.that.does.not.exist; // ERROR cannot.find.class(some.cls.that.does.not.exist)
import another.cls.that.does.not.exist -> another; // ERROR cannot.find.class(another.cls.that.does.not.exist)
import java.lang.Integer -> int;
import java.lang.Void -> void;

class ResolveInvalidImport {
    a: int;
    b: exist; 
    c: another; 
    d: void;
}
// COMPILE