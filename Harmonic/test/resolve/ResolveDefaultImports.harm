package test.resolve;

/*
 */

class Resolve1(parent: Interval) {
    
    bad: IntervalX; // ERROR cannot.resolve(IntervalX)
    
    aRes: Integer;
    interval a(parent) {
        aRes = 22;
    }
    
    bRes: Integer;
    interval b(parent) {
        bRes = 22;
    }
    
    cRes: Integer;
    interval c(parent) {
        cRes = process(aRes + bRes);
    }

    a -> c;
    b -> c;
}

// COMPILE