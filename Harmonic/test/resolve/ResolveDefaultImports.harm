package test.resolve;

class Resolve1(parent: Interval) {
    
    bad: IntervalX = null; // ERROR cannot.resolve(IntervalX)
    
    aRes: Integer = null;
    interval a(parent) {
        aRes = 22;
    }
    
    bRes: Integer = null;
    interval b(parent) {
        bRes = 22;
    }
    
    cRes: Integer = null;
    interval c(parent) {
        cRes = process(aRes + bRes); // ERROR no.such.method(test.resolve.Resolve1, process())
    }

    a -> c;
    b -> c;
}

// COMPILE